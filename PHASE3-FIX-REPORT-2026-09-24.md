# MKLAB Stock — Phase 3 修復報告（2026-09-24）

## ⚠️ 先確認一件事

我重新從 GitHub `main` 分支抓了一份最新原始碼核對，發現 **2026-09-23 那份 Phase 2 修復（`TOP100` 假資料、RSI/EPS成長/均線假指標等）目前還沒有被 commit/push 上去**——`scripts/daily_robust.sh` 還在、`TOP100` 還在。這份 zip 是**疊加在 Phase 2 之上**的完整版本（4 個檔案裡已經包含 Phase 2 的修復內容，不用回頭找舊 zip），你只要套用**這一份**就好，一次到位。

---

## 這一階段新找到並修好的問題

### 新發現：Research 頁的 K 線圖，不管選哪一檔股票，畫的都是大盤指數，不是那檔股票自己的走勢

- **檔案**：`mklab-stock-research.html`
- **問題**：我逐行看過 `klAddIndicator()` 的 MACD／KD 計算邏輯本身——12日/26日 EMA、DIF/DEA、RSV/K/D——都是正確的標準公式，**這部分沒有造假**。但問題出在它算的資料來源：整頁只有 `window.TWII_KDATA`（`data/twii_kdata.js`）這一份資料，是**台股加權指數**的歷史 K 線（數值在 24,000 點上下），**不是**個別股票的價格歷史。也就是說，不管你在左邊選台積電、台泥、還是鴻海，右邊那張「K線圖」畫的永遠是同一份大盤指數走勢，MACD/KD 也是對著大盤算的。
- **原本的標題**寫的是「歷史價格圖（1101 台泥 · TWII 加權指數參考）」——把股票名稱跟大盤指數並列在同一個標題裡，很容易讓人誤以為那是這檔股票自己的價格走勢圖。
- **根因**：資料管線裡只抓了大盤指數的歷史 K 線，沒有幫每一檔個股各自保存歷史 OHLC 序列（`data/history/` 雖然有 302 天的每日快照，但那是「一天一檔全市場」的格式，要重組出單一股票的 1 年線，得下載並合併上百個檔案，對一個網頁請求來說不現實）。
- **已修**：不是造一批假的個股 K 線去填坑，而是把標題**誠實改成**「大盤走勢參考（TWII 加權指數・近 1 年，非個股價格走勢）」，不再放股票名稱，避免任何人誤讀成個股走勢圖。個股自己的即時數字（價格/PE/PB/ROE/EPS/ROA）本來就是真的、放在上面那張卡片，這次沒有動，也不受影響。

### 全站複查（這階段沒有再找到新的假資料）

- `assets/js/*.js`（`mklab-core.js`、`data-client.js`、`mklab-wc.js`）：搜尋 `Math.random`/`SAMPLE`/`MOCK`/`TOP100` 等全部關鍵字，**沒有命中**。
- Breadth 頁市場寬度統計（上漲/下跌家數、產業上漲比例）：直接對 `stocks.json` 的真實 `chg` 欄位做計數，邏輯正確。
- Compare 頁「最優」欄位標記：對使用者實際加入比較的真實股票資料做 max/min 比較，邏輯正確。
- Dividend 頁篩選：直接讀真實殖利率欄位篩選，邏輯正確。
- 全站搜尋 `||0` 這種可能把 `null` 誤植為 `0` 的寫法：確認只剩 Screener 頁的股息欄位還有一處（`s.div||0`），這個是用在篩選比較上，`null` 跟 `0` 在「殖利率≥門檻」這種篩選情境下行為一致，不影響篩選結果正確性，風險很低，這次判斷不需要處理。

修復後跑過 `python3 skills/qa-gate/qa_gate.py`：**0 Critical、ALLOW DEPLOY**；Playwright 測過全站 13 頁 Console 皆無錯誤；實際點擊 Research 頁切換不同股票，確認圖表標題不再隨股票名稱變動、個股資訊卡數字正確跟著切換。

---

## 下一階段可以做的事（不是「錯誤」，是功能規劃，供你參考）

這些都不是「假資料」問題，是「目前資料庫還沒有這份資料，所以功能做不出來」，性質跟 Phase 2 報告最後提到的 RSI／均線／EPS成長率是同一類：

1. **個股真實 K 線圖**：要讓 Research 頁真的畫出每一檔股票自己的價格走勢，需要在 `skills/data/fetch_data.py`（或新增一支腳本）在 Build-Time 從 `data/history/` 的每日快照重組出「每檔股票各自的 1 年 OHLC 序列」，輸出成類似 `data/history-per-symbol/{代號}.json` 的精簡檔案，前端才能在使用者選股時只抓那一檔的資料（而不是抓全部 302 天 × 1800 檔的原始快照）。
2. **真正的 MA／RSI**：有了第 1 點的個股歷史序列，就能在同一支 Build-Time 腳本裡順便算出真正的 5/10/30 日均線和 RSI，寫回 `stocks.json`，Screener 頁被我這次拿掉的均線/RSI 篩選就能真的做出來，而不是繼續空著。
3. **EPS 成長率**：需要追蹤至少兩期（例如近四季）的 EPS 歷史，目前 `stocks.json` 只存最新一筆快照，要另外設計一個「EPS 歷史序列」的資料結構才能算年增率/季增率。

這三項工程量都不小，屬於「新增資料管線能力」，不是這幾輪「抓假資料、改成誠實」的除錯範圍，建議你評估優先順序後我們再另外排一輪來做。

如果你想先確認我這次列的三項規劃是否合理、要不要調整順序，或想先從哪一項開始，跟我說一聲即可。
