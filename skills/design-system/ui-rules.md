# UI Rules（介面規則）

1. 所有顏色來自 Design Token（`var(--*)`），禁止硬寫 `#hex` 於核心樣式
2. 漲跌：紅=漲、綠=跌
3. 元件優先使用 `<mklab-*>` Web Components
4. 響應式：mobile.css 處理窄屏
5. 字體系統一致，不混用多套
6. 間距使用統一尺度（4/8/12/16/24px）
7. Dark/Light 雙主題必須皆可用
