# mklab-stock 修復說明（2026-09-19）

這份 zip 包含**這一輪改動過的檔案**：

```
mklab-stock-backtest.html      ← 回測功能修復 + 資料範圍提示
mklab-stock-breadth.html       ← 漲跌幅分布條狀圖修復
mklab-stock-portfolio.html     ← 新增「持股佔比／產業分類／成分股」三分頁圖表
assets/css/layout.css          ← 全站表格留白加大、資料列文字置中、卡片間距、hover 過場動畫
```

---

## 1. 全站股票表格樣式統一

實際稽核後發現全站表格本來就共用同一套全域 CSS（`th, td` 樣式、`tbody tr:hover` 變色、奇偶行網底），只有 Compare 頁一個表格因為版面需求做了合理的置中覆寫，並不是「每個表格都不一樣」。判斷實際的感受落差主要來自留白不夠緊繃，已調整：

- 表格 cell 的 padding 由 `10px 8px` 加大為 `12px 12px`，文字與欄位不會太擠。
- `tbody tr` 加上 `transition`，滑鼠移過去變色時是平滑過場，不是硬切換。
- 首欄（股票代號/名稱等）維持靠左、其餘數字欄位靠右，這個規則原本就是全站一致的。

## 2. Portfolio／Help／Log 頁區塊間距

這三頁的每個說明區塊都是獨立的 `.card`，但 `.card` 本身沒有定義卡片之間的間距，直接堆疊會完全貼在一起。已新增規則，讓 `.wrap` 底下連續出現的 `.card` 之間自動留白（只影響這種「直接堆疊的獨立卡片」，不會影響首頁 `.cards`／Portfolio `.pf-summary` 這種用 grid `gap` 排列、本來就有間距的卡片群組）。

## 3. 資料列文字置中

`.freshness`（「資料以 2026-09-16 收盤為準…」那一列）原本沒有設定 `text-align`，預設靠左。已加上 `text-align: center;`。

## 4. Breadth 頁漲跌幅分布沒有顏色/條狀圖

**根因**：條狀圖的顏色與寬度其實都算對了（`--bar-fill` 顏色、`width:97.9%` 這類數值都正確），但撐條的 `<span class="breadth-bar-fill">` 沒有設定 `display: block`。`<span>` 預設是 `display: inline`，瀏覽器會直接忽略 inline 元素的 `width` / `height`，所以整條「柱狀圖」其實一直是寬高 0、完全隱形的狀態，不是資料或顏色的問題。加上 `display: block;` 後，紅／綠色的分布條狀圖就正常顯示了（紅漲、綠跌，符合台股慣例）。

## 5. Backtest 回測功能修復

**根因**：`data/history/*.json` 裡每筆股票的欄位是 `stock_id`，但回測程式碼寫成 `histRows.find(r => String(r.sym) === sym)` 去比對——這個 `r.sym` 欄位在歷史快照資料裡根本不存在（歷史資料的欄位叫 `stock_id`，不是 `sym`），所以無論選哪個股票、哪個日期，這個比對永遠找不到，回測**必然失敗**，跟日期範圍完全無關。已改成用正確的 `stock_id` 欄位比對，實測 0050 + 2025-07-14 已能正常算出報酬率、年化報酬率。

另外也順便更正一個認知：資料庫歷史快照其實只從 **2025-07-14** 到現在（約 14 個月），不是「近 3 年」。已在頁面：
- 幫日期選擇器加上 `min="2025-07-14"` 限制，避免選到範圍外的日期。
- 在說明文字裡明確標出「歷史快照範圍為 2025-07-14 至今」。

## 6. Portfolio 新增三種圖表分頁

在「持股佔比」卡片上方新增一個分頁切換（沿用網站既有的 `.seg` 分段按鈕樣式）：

- **持股佔比**：跟原本一樣，依個股市值佔總市值的比例畫圓餅圖。
- **產業分類**（新增）：把持股依 `stocks.json` 裡每檔股票的產業分類（`ind` 欄位）加總市值後畫圓餅圖，可以看出整個投資組合在各產業的曝險比例。
- **成分股**（新增，誠實告知現況）：這個功能需要「各 ETF 實際持有哪些股票、各佔多少權重」的資料，但**目前 repo 的 `data/` 裡完全沒有這份資料**（`etf-shares.json` 只有 ETF 發行張數，不是成分股權重）。所以這個分頁目前會：
  - 自動偵測持股中代號以 `00` 開頭的 ETF（例如 0050、006208、00981A——採用代號慣例判斷，因為 `stocks.json` 的 `is_etf` 欄位目前標記不準確，例如一般股票「2883 凱基金」被標成 `true`，真正的 ETF「0050」卻是 `false`，不能拿來用）。
  - 如果偵測到你持有 ETF，會列出偵測到的 ETF 名稱，並清楚說明「還缺成分股權重資料，需要額外建置一個定期抓取各 ETF 公開說明書／月報持股明細的資料源，才能在這裡合併算出整體成分股曝險比例」。
  - **沒有偷偷編造假資料**——這是需要新資料管線的功能，目前先把分頁位置與說明做好，資料源建置完成後即可直接把圖表接上去。

如果之後想真的把「成分股」這頁做出來，需要的是：找一個穩定、免key、可定期抓取的 ETF 持股明細來源（例如各投信官網或公開資訊觀測站的 ETF 月報 PDF/頁面），寫一支類似 `skills/data/fetch_data.py` 的抓取腳本，產出類似 `data/etf-holdings.json`（格式建議：`{ etf_sym: [{stock_id, weight_pct}, ...] }`），前端就能照抓 `stocks.json` 的方式讀取並加總計算。
