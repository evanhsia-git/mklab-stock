# MKLAB Stock — 技術指標（RSI/MACD/KD/均線）實作報告（2026-09-25）

## 這次做了什麼

新增一支 Build-Time 計算腳本 `skills/data/compute_indicators.py`，**完全基於已經在抓的 `data/history/` 逐日快照做本地數學計算**，不會多打一次外部 API、不會新增任何前端要下載的檔案——計算結果就是幾個數字欄位，直接寫進 `data/stocks.json` 每一檔股票原本就有的紀錄裡。這是這次你要求「最快能完成」的項目裡，投入產出比最高的一個，所以優先做了它。

算出的指標：

| 欄位 | 說明 | 需要的歷史天數 |
|---|---|---|
| `ma5` / `ma10` / `ma20` / `ma30` | 5/10/20/30 日簡單移動平均線 | 5～30 天 |
| `rsi14` | 14 日 RSI（相對強弱指標） | 15 天 |
| `macd_dif` / `macd_dea` / `macd_hist` | MACD 的 DIF、訊號線 DEA、柱狀圖 | 35 天 |
| `kd_k` / `kd_d` | KD 隨機指標（9 日 RSV，台股慣例 K/D 初始值 50） | 9 天 |

資料不足的股票（例如剛上市不久），對應欄位一律輸出 `null`，不會用其他數字冒充——這點延續了前幾輪「資料不足即為 null」的原則。

## 已經接上前端的地方

1. **Screener「技術面」篩選恢復成真的可以用**：RSI(14)≥ 滑桿、均線下拉選單重新出現在篩選表單裡，這次接的是真實計算出來的數字（「站上5MA」現在真的是「目前股價 ≥ 5日均線」，不再是「今天有沒有漲」那個假邏輯）。
2. **「動能」策略範本恢復成原本設計的樣子**：RSI(14)≥60 + 單日漲幅≥1% + 站上5MA，說明表格文字也一併更新回對應真實條件的描述。
3. **Research 頁個股頁面新增技術面資訊**：在原本的價格/PE/PB/ROE 卡片下方，新增一排 RSI(14)／MA5／MA20／MACD柱狀／KD-K／KD-D，跟著你選的股票即時切換（這次修復過程中還抓到一個舊 bug：`loadAll()` 組資料時本來只挑了 name/price/pe/pb/roe/roa/eps 這幾個欄位，會把新指標欄位過濾掉，已經一併補上）。

## 自動化排程

`.github/workflows/daily-update.yml` 的 `daily` job 裡，在「抓當日收盤」之後、commit 之前，新增一個步驟執行 `compute_indicators.py`，之後每天排程跑完抓資料，會自動重新算一次技術指標，不需要人工介入。

## 實測結果

- `python3 skills/qa-gate/qa_gate.py`：0 Critical、ALLOW DEPLOY
- 實際對我本地已有的 306 天歷史資料跑過一次：1380 檔股票裡 1377 檔算出 RSI、1368 檔算出 MACD（其餘因歷史不足正確顯示 null）
- 抽查台積電（2330）：股價 2480、MA5~MA30 在 2412~2430 之間、RSI 60、MACD 柱狀 +2.65、KD 56.1/41.7——股價站上所有均線、指標同步偏多，內部邏輯一致，數字合理
- Screener 篩選「RSI≥70」：1380 檔中篩出 108 檔；疊加「站上30MA」再篩到 107 檔——確認是真的在篩選，不是像以前那樣全部套用同一個假數字
- 全站 13 頁 Playwright 測過，Console 無錯誤

## 這份 zip 額外包含 `data/stocks.json`

因為我本地已經有一份 306 天的歷史資料可以直接拿來算，所以這次**直接把算好指標的 `stocks.json` 也放進 zip**，這樣你套用後立刻就能在網頁上看到真實數字，不用等到下一次排程跑完。之後只要 `compute_indicators.py` 有進到 workflow 裡，之後每天都會自動重新計算、自動覆蓋，你不用手動維護這個檔案。

---

## 關於個股真實 K 線圖的評估結論

你要求「評估每一檔個股如要產生 K 線會不會造成網站檔案大小或執行負擔，如果會就放棄」。我重新評估後的結論是：**不會造成負擔，值得保留這個功能，但這次沒有動手做（照你說的放在最後）**。

上一輪報告裡我原本以為要做個股 K 線，得讓前端去下載、拼湊 `data/history/` 裡上百個「一天全市場」的快照檔案（這樣確實會很重，單一股票可能要抓好幾十 MB）。但重新想過一次，正確做法應該跟這次做指標計算一樣：**在 Build-Time 用同一份 `compute_indicators.py` 順便把每一檔股票自己的 OHLC 序列，各自寫成一個小檔案**（例如 `data/kline/2330.json`），使用者選哪一檔，前端就只抓那一檔的小檔案。抓 90 天的資料，一檔大概 6KB 上下；1380 檔全部產生出來，總共大約 9MB，跟現有 `data/history/` 資料夾的 90MB 比起來完全不算負擔，使用者每次只會抓到自己要看的那一檔，不會有額外負擔。

所以結論是：**不建議拿掉這個功能**，等你要排下一輪時再實作即可，屆時 Research 頁的圖表就能顯示每一檔股票自己真正的價格走勢，不用再顯示「大盤走勢參考」那個目前的替代方案。
